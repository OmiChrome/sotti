Implement the following classes ensuring a **deep copy** using the `Cloneable` interface:

1.  **`Address`**: `String` variable, constructor, accessor, and `clone()` method.
2.  **`Department`**: `String` variable, constructor, accessor, and `clone()` method.
3.  **`Person`**: `String name`, `Address addr`. Constructor, accessors, and `clone()` (must clone `addr`).
4.  **`Employee`** (extends `Person`): `Department dept`. Constructor, accessors, and `clone()` (must clone `super` and `dept`).
    *   Implement `updateEmp(String newAddr, String newDept)` to update the employee's address and department.
    *   Override `toString()` to return the format: `name : address : department`.

**Resembles**: `Q1_Admission.java` and `Q8_PublicationSystem.java` (Deep Copy logic), but uses `Cloneable` instead of copy constructors.

**Edge Case**: Ensure `clone()` methods are `public` and handle `CloneNotSupportedException`. The `updateEmp` method must modify the object such that the cloned object `e2` remains unchanged.